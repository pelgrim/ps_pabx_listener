require 'spec_helper'

describe PsPabxListener do
  it 'has a version number' do
    expect(PsPabxListener::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
