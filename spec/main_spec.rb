require 'spec_helper'

describe PsPabxListener::Main do
  it 'requires a host'
  it 'requires a username'
  it 'requires a password'
  it 'creates a valid instance'
  it 'gets data until a given timeout is reached'
  it 'continuously gets data until its required to stop'
end
